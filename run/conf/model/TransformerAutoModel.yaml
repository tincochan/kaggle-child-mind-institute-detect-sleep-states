name: TransformerAutoModel
params:
  model_name: D:\kaggle-child-mind-institute-detect-sleep-states\deberta-v3-small #  推論時はinternet offなので、datasetから取得したconfigを使う
  hidden_size: 120
  stride: ${downsample_rate}
